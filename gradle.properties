org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Configuration cache left off: it can serve a stale native (CMake/Prefab)
# model after a pull, masking build-config changes. Re-enable once stable.
